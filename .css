```css
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
}

.container {
    width: 80%;
    margin: 20px auto;
}

h1 {
    text-align: center;
}

#search-options {
    text-align: center;
    margin-bottom: 20px;
}

input[type="text"] {
    padding: 8px;
    font-size: 16px;
}

button {
    padding: 8px 16px;
    font-size: 16px;
    cursor: pointer;
}

#results {
    list-style-type: none;
    padding: 0;
}

#results li {
    margin-bottom: 10px;
}
```

**script.js:**

```javascript
function scanQRCode() {
    // QRコードをスキャンする処理を実装する
    alert("QRコードをスキャンします。");
}

function searchByLocation() {
    const location = document.getElementById("location").value;
    // 入力されたエリアで加盟店を検索する処理を実装する
    alert(location + "の加盟店を検索します。");
}
```
